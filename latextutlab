#include <stdio.h>
int main()
{
    int x = 5;
}
// ls -a shows hidden files
//git rm que.c --cached
// when dealing with repositories, the TA got stuck on git pull vs git push, it was git pull that was needed
// stick to origin master
// there is no such thing as origin, need remote code, but we only need that if it doesn't work
// git initgit remote set url first